# Add project specific ProGuard rules here.
# Minification is disabled for the release build type (isMinifyEnabled = false),
# so these rules are currently inert. They exist because the release buildType
# in app/build.gradle.kts references this file via proguardFiles(...).
